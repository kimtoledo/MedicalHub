import 'server-only';
import { cookies } from 'next/headers';
import { getBackendUrl } from './backend';
export type AppointmentStatusEvent = { id: string; fromStatus: string | null; toStatus: string; changedByName: string | null; reason: string | null; createdAt: string };
export type AppointmentDetail = { id: string; branchId: string; branchName: string; branchAddress: string | null; branchCity: string | null; branchProvince: string | null; patientId: string | null; patientFirstName: string; patientLastName: string; patientNumber: string | null; patientPhone: string | null; patientEmail: string | null; dentistId: string | null; dentistFirstName: string | null; dentistLastName: string | null; serviceId: string | null; serviceName: string | null; serviceWorkflowMode: 'quick' | 'standard' | null; status: string; startsAt: string; endsAt: string | null; chiefComplaint: string | null; notes: string | null; cancellationReason: string | null; confirmedAt: string | null; checkedInAt: string | null; completedAt: string | null; cancelledAt: string | null; statusHistory: AppointmentStatusEvent[] };
async function clinicGet<T>(path: string, params: Record<string, string | number>) { const url = getBackendUrl(path); Object.entries(params).forEach(([key, value]) => url.searchParams.set(key, String(value))); const response = await fetch(url, { headers: { cookie: cookies().toString() }, cache: 'no-store' }); if (!response.ok) throw new Error(`Clinic API failed with status ${response.status}`); return ((await response.json()) as { data: T }).data; }
export const getClinicAppointment = (clinicId: string, appointmentId: string) => clinicGet<AppointmentDetail>(`/v1/clinic/appointments/${encodeURIComponent(appointmentId)}`, { clinicId });
