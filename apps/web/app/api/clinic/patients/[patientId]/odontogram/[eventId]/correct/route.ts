import type { NextRequest } from 'next/server'; import { proxyToApi } from '@/lib/api-proxy'; type Context = { params: Promise<{ patientId: string; eventId: string }> }; export async function POST(request: NextRequest, context: Context) { return proxyToApi(request, `/v1/clinic/patients/${encodeURIComponent((await context.params).patientId)}/odontogram/${encodeURIComponent((await context.params).eventId)}/correct`); }
