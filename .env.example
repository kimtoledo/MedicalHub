# =============================================================================
# ToothHub PH — Environment Variables
# Copy this file to .env for local development.
# NEVER commit .env or any file containing real secrets.
# On Replit: use Replit Secrets instead of .env files.
# =============================================================================

# ---------------------------------------------------------------------------
# Database (required)
# ---------------------------------------------------------------------------
# Replit PostgreSQL connection string.
# Get the value from Replit Secrets > DATABASE_URL.
# For local VS Code / Codex dev: copy the value here.
DATABASE_URL=postgresql://user:password@host:5432/dbname

# ---------------------------------------------------------------------------
# Application URLs
# ---------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:5000
NEXT_PUBLIC_API_URL=http://localhost:3001

# Session secret for cookie signing (generate with: openssl rand -hex 32)
SESSION_SECRET=change-me-generate-a-random-secret

# ---------------------------------------------------------------------------
# Auth (Better Auth — added when auth is scaffolded)
# ---------------------------------------------------------------------------
# BETTER_AUTH_SECRET=
# BETTER_AUTH_URL=http://localhost:3001

# ---------------------------------------------------------------------------
# Storage (added when object storage is scaffolded)
# ---------------------------------------------------------------------------
# STORAGE_BUCKET=
# STORAGE_ACCESS_KEY=
# STORAGE_SECRET_KEY=
# STORAGE_ENDPOINT=
